From 7120385c9e5f664f1f66c41d72feab5383a6e575 Mon Sep 17 00:00:00 2001
From: Hannes Schueller <hannes@yllr.net>
Date: Fri, 26 Dec 2014 11:51:00 +0100
Subject: [PATCH 09/20] bugfix: if the status bar is hidden, don't make it
 appear again when using tab completion

---
 main.c | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/main.c b/main.c
index d70d508..e22c8dc 100644
--- a/main.c
+++ b/main.c
@@ -994,7 +994,8 @@ complete(const Arg *arg) {
         gtk_box_pack_start(gui->box, GTK_WIDGET(top_border), FALSE, FALSE, 0);
         gtk_container_add(GTK_CONTAINER(table), GTK_WIDGET(_table));
         gtk_box_pack_start(gui->box, GTK_WIDGET(table), FALSE, FALSE, 0);
-        gtk_widget_show_all(GTK_WIDGET(gui->window));
+        gtk_widget_show_all(GTK_WIDGET(table));
+        gtk_widget_show_all(GTK_WIDGET(top_border));
         if (!n)
             return TRUE;
         current = arg->i == DirectionPrev ? n - 1 : 0;
-- 
2.1.4

